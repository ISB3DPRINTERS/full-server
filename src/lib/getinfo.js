const getcost = (grade) => {
    // ask for cost per grade
    return ("not developed yet"+grade);
  }
const getkey = (grade) => {
    // ask for key per grade
    return ("not developed yet"+grade)
}
var getinforeturn = {getcost, getkey}
export default getinforeturn
